//#region src/get-commit-info.d.ts
interface CommitInfoOptions {
  commit: string;
  repo: string;
}
interface CommitInfo {
  commit: {
    sha: string;
    url: string;
    markdownLink: string;
  };
  author?: {
    login: string;
    url: string;
    markdownLink: string;
  };
  pull?: {
    number: number;
    url: string;
    markdownLink: string;
  };
}
/**
 * Get the information of a specific commit in a GitHub repository. Returns
 * `undefined` if the commit is not found or the GitHub repository doesn't exist.
 */
declare function getCommitInfo(options: CommitInfoOptions): Promise<CommitInfo | undefined>;
//#endregion
//#region src/get-pull-request-info.d.ts
interface PullRequestInfoOptions {
  pull: number;
  repo: string;
}
interface PullRequestInfo {
  pull: {
    number: number;
    url: string;
    markdownLink: string;
  };
  author?: {
    login: string;
    url: string;
    markdownLink: string;
  };
  commit?: {
    sha: string;
    url: string;
    markdownLink: string;
  };
}
/**
 * Get the information of a specific pull request in a GitHub repository. Returns
 * `undefined` if the pull request is not found or the GitHub repository doesn't exist.
 */
declare function getPullRequestInfo(options: PullRequestInfoOptions): Promise<PullRequestInfo | undefined>;
//#endregion
export { type CommitInfo, type CommitInfoOptions, type PullRequestInfo, type PullRequestInfoOptions, getCommitInfo, getPullRequestInfo };