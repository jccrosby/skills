interface:
  display_name: 'adr-create'
  short_description: 'Draft an Architectural Decision Record (ADR). Use when proposing a technology change, capturing a decision that affects multiple teams/processes, superseding a prior decision, or when the user asks for a ADR / decision record. Produces a markdown draft file only.'
