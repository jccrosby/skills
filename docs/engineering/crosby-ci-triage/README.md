# Triage a CI failure

Use this skill when a build or pipeline fails and the responsible layer is unclear.

It finds the first actionable failure and classifies it as change-caused, flaky, environmental, pipeline configuration, or unknown. It reports evidence and the next action without rerunning or changing remote workflows unless requested.
