# Plan a dependency upgrade

Use this skill when upgrading a library, framework, runtime, or toolchain may require migration work.

It combines repository usage with authoritative compatibility and release information, then returns ordered changes, verification, risks, and rollback limits. It does not change manifests or lockfiles unless requested.
